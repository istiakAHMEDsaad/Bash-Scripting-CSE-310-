# less [file.txt] 	--{for see the large text file}
# 'space key' 		--{view page by page}
# 'shift' + 'g'		--{end of the page}
# '1' + 'g' / 'g'		--{begging of the file}
# '/' [word]			--{word name to find in the text file}
# '?' [word] then 'n' 	--{show list}
