# cd [directory name]

# 1. cd /	--{root directory}

# 2. cd ~ 	--{home directory}

# 3. cd .. 	--{parent of the directory}

# 4. cd [file name]	--{go to given name directory}

# 5. cd My\ folder,
#    cd "My folder",
#    cd 'My folder'	--{if file name or directory contain white space you can use this three methods, because cd looking for the first argument, if you provide space it's recognize as two argument}


