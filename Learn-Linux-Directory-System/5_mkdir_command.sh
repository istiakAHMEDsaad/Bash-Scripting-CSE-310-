# mkdir [directory aka file name]	--{create a folder make directory}

# example => 

# mkdir [folder-name]

# mkdir [folder-name/sub-folder-name]	--{by adding / you can add sub folder but you need parent directiry first}

# mkdir -p folder/folder		--{p flag force create a folder with sub folder}
# mkdir --parents folder/folder 	--{same work}

# mkdir -p folder/{folder1,folder2,folder3}	--{many sub directory, don't use comma}
