# [output] > [file name]

# cat > [file-name.extension] 	--{write in text file directly [press 'Ctrl' + 'D'] to stop cat}

# cat [file-name.extension] 	--{to see what inside the text file}

# cat >> [file-name.extension]	--{append new text to previous file}

# cat [file-name.extension] [file-name.extension] > [output-file-name.extension] 	--{this will create a new file which content file 1, file 2 data}


