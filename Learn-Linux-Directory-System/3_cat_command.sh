# cat [displaying text file] [combining copies file] [creating new text file]
# Example =>

# cat	--{single cat command without any option this cat only print echo}
#	*press 'Ctrl' + 'D' to stop from cat [^D]

# cat [file-name.extension] 	--{display what inside a text file}

# cat [file-name1.extension] [file-name2.extension] 	--{display both text file content}

# cat -b [file-name.extension] 	--{it's add the line number to nonblank line}

# cat -n [file-name.extension]	--{it's add line number to all the lines *count blank also}

# cat -s [file-name.extension]	--{it's squeezes blank line to one blank line}

# cat -E [file-name.extension] 	--{it's add the dollar symbol at the end of each line}

# man cat 	--{show all documentation of cat functionality}
